# Detection-of-a-Cycle-in-Undirected-Graph-using-BFS
This C++ program builds an undirected graph using an adjacency list and checks for cycles. It uses DFS with parent tracking to detect if a visited neighbor (not the parent) exists, indicating a cycle. The program handles disconnected components and finally reports whether a cycle is present.
